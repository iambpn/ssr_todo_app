{% extends "base/base_with_header.njk" %} {% block body %}
<div class="d-flex w-100 align-items-center flex-column">
  <div class="w-50 border rounded px-2 py-3" style="min-height: 100px">
    <div class="">
      <h4 class="px-3">Add Todo</h4>
    </div>
    <div class="p-3">
      <form action="/add" method="post" id="todoEditor">
        <div class="mb-3">
          <label for="name" class="form-label">Name</label>
          <input
            type="text"
            class="form-control"
            id="name"
            placeholder="name"
            name="name"
            onchange="validateLength('name', 2, 'Name must be greater than 2 characters')"
          />
          <div class="valid-feedback invalid-feedback"></div>
        </div>
        <div class="mb-3">
          <label for="description" class="form-label">Description</label>
          <input
            type="text"
            class="form-control"
            id="description"
            placeholder="description"
            name="description"
            onchange="validateLength('description', 2, 'Description must be greater than 2 characters')"
          />
          <div class="valid-feedback invalid-feedback"></div>
        </div>
        <div class="mb-3">
          <label for="scheduled_date" class="form-label">Scheduled Date</label>
          <input
            type="date"
            class="form-control"
            id="scheduled_date"
            placeholder="scheduled_date"
            name="scheduled_date"
            onblur="validateEmpty('scheduled_date', 'Schedule Date is required')"
          />
          <div class="valid-feedback invalid-feedback"></div>
        </div>
        <div class="d-flex justify-content-end px-2">
          <button type="submit" class="btn btn-primary">Add Todo</button>
        </div>
      </form>
    </div>
  </div>
</div>
{% endblock %} {% block script %} {{ super() }}
<script src="/public/script/validation.js"></script>
{% endblock %}
